# FlashAttention
This project simulates and benchmarks the evolution of attention mechanisms—from the standard (naive) PyTorch implementation to FlashAttention (FA1, FA2, and FA3). It visualizes the exact moment standard attention runs out of memory and demonstrates how FlashAttention rescues long-sequence modeling
